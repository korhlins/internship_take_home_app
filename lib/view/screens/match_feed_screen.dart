import 'package:flutter/material.dart';

class MatchFeedScreen extends StatelessWidget {
  const MatchFeedScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Match Feed Screen"),
    );
  }
}
