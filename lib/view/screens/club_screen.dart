import 'package:flutter/material.dart';

class ClubFeedScreen extends StatelessWidget {
  const ClubFeedScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Club Feed Screen"),
    );
  }
}
